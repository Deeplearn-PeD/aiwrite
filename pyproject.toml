[tool.poetry]
name = "aiwrite"
version = "0.1.1"
description = ""
authors = ["Flávio Codeço Coelho <fccoelho@gmail.com>"]
readme = "README.md"
packages = [
    { include = "aiwrite", from = "." }
]

[tool.poetry.scripts]
libbygui = "aiwrite.main:run"

[tool.poetry.dependencies]
python = ">=3.12,<3.13"
flet = ">=0.25.2"
libby = {git = "https://github.com/Deeplearn-PeD/libby"}
sqlmodel = ">=0.0.18"
python-dotenv = "^1.0.1"


[tool.poetry.group.dev.dependencies]
aider-chat = "^0.70.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
