[tool.poetry]
name = "libbygui"
version = "0.1.0"
description = ""
authors = ["Flávio Codeço Coelho <fccoelho@gmail.com>"]
readme = "README.md"
packages = [
    { include = "libbygui", from = "." }
]

[tool.poetry.scripts]
libbygui = "libbygui.main:run"

[tool.poetry.dependencies]
python = "^3.11"
flet = "^0.22.0"
libby = {git = "https://github.com/Deeplearn-PeD/libby"}
sqlmodel = "^0.0.16"
python-dotenv = "^1.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
